<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>title</title>
</head>
<body>
    <h1>hello board_view</h1>
    <div>
    <table border="1">
        <tr>
            <th>번호</th>
            <th>제목</th>
            <th>내용</th>
            <th>시간</th>
            <th>삭제</th>
        </tr>
        <% for(let i = 0; i < data.length; i++){ %> 
            <tr>
                <td><%= data[i].id %></td>
                <td><%= data[i].title %></td>
                <td><%= data[i].content %></td>
                <form action="">
                <td><a href="/delete/<%= data[i].isd %>">삭제</a></td>
                <!--<td><button onclick="location.href = '/delete/<%= data[i].id %>'">삭제</button></td>-->
            </tr>
        <% } %>
    </table>
</div>
</body>
</html>